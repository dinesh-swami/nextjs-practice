'use client'
import React from 'react'

const page = async () => {
  return (
    <div>
      hello
      <button onClick={()=> alert('hello world!')}>test</button>
    </div>
  )
}

export default page
